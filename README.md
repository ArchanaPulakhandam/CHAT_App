# CHAT_App
A real-time private chat application built with Spring Boot and WebSocket (STOMP + SockJS). Supports user authentication, private messaging, message persistence, and dynamic user search with a WhatsApp-style UI.
